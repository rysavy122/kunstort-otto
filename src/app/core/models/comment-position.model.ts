export interface CommentPosition {
  kommentarId: number;  // Changed to match C# naming convention
  xPosition: number;    // Changed to match C# naming convention
  yPosition: number;    // Changed to match C# naming convention
  borderColor: string;  // Changed to match C# naming convention
}
